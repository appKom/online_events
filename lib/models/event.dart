// class Event extends 

// This model has everything the list event has, and the following properties:
// Description
// Location
// Comitee
// (Date with time)
// Waitlist

// When loading the page we need to figure out a couple of things:
// Has the registration opened? If not, show a timer (påmelding åpner ..., )
// Is the user registered for this event? If not, show the 'meld meg på' button and the 'Se påmeldte' button
// If user is registered, there should be an option to 